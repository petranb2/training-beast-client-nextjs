export interface TrainingInitialValuesModel {
    _id?: string | null,
    name: string,
    comments: string,
    multiSection: boolean,
    cyclic: boolean,
    date?: string | null,
    status: string
}